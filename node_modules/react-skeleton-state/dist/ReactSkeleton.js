"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
const react_1 = __importDefault(require("react"));
const ReactSkeleton = ({ children, width, height, variant, borderRadius, backgroundColorStart, backgroundColorEnd, fontSize }) => {
    const rectangle = {
        borderRadius: "4px"
    };
    const circle = {
        borderRadius: "1000px"
    };
    const text = {
        width: "100%",
        borderRadius: "4px"
    };
    const fadeInAnimation = `
    @keyframes reactSkeletonAnimate{
          from {
              background: ${backgroundColorStart ? backgroundColorStart : '#efefef'};
          }
          to {
              background: ${backgroundColorEnd ? backgroundColorEnd : '#e4e4e4'};
          }
      }
  `;
    const animationStyle = {
        animation: 'reactSkeletonAnimate 1s 0s linear infinite alternate',
    };
    const findVariant = () => {
        switch (variant) {
            case 'circle':
                return circle;
            case 'rectangle':
                return rectangle;
            case 'text':
                return text;
            default:
                return rectangle;
        }
    };
    return (react_1.default.createElement(react_1.default.Fragment, null,
        react_1.default.createElement("style", null, fadeInAnimation),
        react_1.default.createElement("div", { className: 'react-skeleton', style: Object.assign(Object.assign(Object.assign(Object.assign(Object.assign({}, animationStyle), (findVariant())), (borderRadius ? { 'borderRadius': borderRadius + "px" } : {})), { width: width }), (fontSize ? { 'height': fontSize } : { height: height })) }, children)));
};
exports.default = ReactSkeleton;
